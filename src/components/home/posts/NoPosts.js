import React from "react";

import "./Loader.css";

const NoPosts = props => (
  <div className="post-loader-container" style={{ color: "white" }}>
    <div>Nothing here! Be the first to create a post in this category</div>
  </div>
);

export default NoPosts;
